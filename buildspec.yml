version: 0.2

phases:
  build:
    commands:
      - java -version
      - java -version
      - mvn install && mvn verify sonar:sonar

artifacts:
  files:
    - target/spring-boot-microservice-forex-service-0.0.1-SNAPSHOT.jar

  discard-paths: yes